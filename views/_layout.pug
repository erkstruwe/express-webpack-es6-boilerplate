doctype html
html(lang="en")

head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no")
    title JavaScript 101
    script(src=`${config.urlPrefix}${webpackConfig.output.publicPath}/index.js`)
    block head

body
    .container-fluid
        block body
